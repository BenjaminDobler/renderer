import { AnimatableDirective } from './animatable.directive';

describe('AnimatableDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimatableDirective();
    expect(directive).toBeTruthy();
  });
});
